# claude-skills

Personal index of [Claude Code](https://claude.com/claude-code) skills and commands I use. **Each native skill lives in its own repo** — this archive curates them, plus keeps pinned snapshots of upstream bundles I rely on.

## Native skills

Each one is a standalone repo. Install with the command on its own README.

| Skill | Repo | What it does |
|---|---|---|
| `ascii-diagram` | [fbdeme/ascii-diagram](https://github.com/fbdeme/ascii-diagram) | CJK-safe ASCII/box-drawing text diagrams in markdown/chat (architecture, org/ownership structure, pipelines). Hangul occupies 2 display columns, so eyeballing fails: rails-first drawing grammar (solid=primary / dashed=secondary, labels right of rails), then **verify with `scripts/check_align.py`** — East-Asian-width-aware column check that detects rail "jogs" (the stray-space bug) and bans ambiguous-width chars (`·` `①` `㈜` `—`). For quick in-doc diagrams where `tikz-figure` is overkill; companion to it. |
| `academic-paper-reviewer` | [fbdeme/academic-paper-reviewer](https://github.com/fbdeme/academic-paper-reviewer) | Multi-perspective academic paper peer review — EIC + 3 reviewers + Devil's Advocate, field-aware. Full / re-review / quick / methodology-focus / Socratic / calibration modes. (Ships with bundled `shared/` dependency.) |
| `academic-register` | [fbdeme/academic-register](https://github.com/fbdeme/academic-register) | Fix English academic prose **register**, guarding BOTH poles at once: too-informal (contractions, idioms, phrasal verbs, "a lot of", punchy fragments) ↔ AI-inflated (delve, underscore, tricolons, "not only… but also", "it is worth noting", hedge clusters). The insight is that a naive "make it formal" pass pushes A→B, so the both-pole AI-slop intersection is off-limits in *both* directions. Severity rubric (R1–R10 + Hyland stance: assert facts, hedge mechanisms; Sword zombie nouns), deterministic `scripts/register_scan.py` (trigger words + em-dash/tricolon density — high-precision, low-recall on open-ended idiom, so the human R5/R10 pass is the real net), and a cited `references/research_brief.md`. English scholarly-register sibling of `korean-humanizer`; pairs with `prose-readability` / `stat-tone`. |
| `claude-state-migrate` | [fbdeme/claude-state-migrate](https://github.com/fbdeme/claude-state-migrate) | Migrate & merge Claude Code's own state — chat sessions (`.jsonl`) + auto-memory (`.md`) — between machines or project scopes without overwriting. Version-aware cwd path-encoding, scope-preserving memory merge with migration banners + perspective translation, optional in-transcript path rewrite. Helper script is dry-run by default. |
| `claude-tmux-ssh` | [fbdeme/claude-tmux-ssh](https://github.com/fbdeme/claude-tmux-ssh) | Durable, auto-named tmux workflow for Claude Code over SSH (Linux/systemd): SSH stays a plain shell, `claude` runs in its own disconnect-surviving tmux session auto-renamed to `claude-<id>` so `tmux ls` matches `claude --resume`. Bundles a user systemd `tmux.service`, a `claude` shell wrapper, and a SessionStart rename hook; idempotent dry-run installer. Companion to `claude-state-migrate`. |
| `cq-extraction` | [fbdeme/cq-extraction](https://github.com/fbdeme/cq-extraction) | Extract Competency Questions + atomic SubCQs from business workflow docs (AS-IS flowcharts, process sheets) for ontology construction. 3-layer model (step/CQ/SubCQ — step↔CQ is N:M, CQs belong to reusable semantic blocks, SubCQ = exactly one relation = one SPARQL pattern with ontology role tags `주어(C)→술어(OP\|DP\|AP)→목적(C\|L)`), CQ typing (query-type / judgment-type = ontology+data joint contract with the judgment's criteria vocabularized as annotations / cross-cutting code-semantics), evidence-grade gate (no CQs from weak evidence), and a pitfalls section earned from a real PCB production-management build. Grows a case log per application. Companion to `webprotege-cli` (authoring side). |
| `docs-pattern` | [fbdeme/docs-pattern](https://github.com/fbdeme/docs-pattern) | Bootstrap and maintain a five-file project-docs pattern under `docs/` (`current_status` / `history` / `issues` / `todo` / optional `research_method`). Explicit reads/writes only — no hooks. |
| `prose-readability` | [fbdeme/prose-readability](https://github.com/fbdeme/prose-readability) | Measure-then-de-verbose audit for academic prose (papers, rebuttals, SOPs): `scripts/density.py` counts em-dashes in prose vs in headers, per-1k dash density, semicolons, parentheticals, and 40+ word sentences — then the fix is **moving** text so each claim carries its caveat once, never adding hedges. Externalized from `academic-paper-reviewer` v2.1 (consulted via its `external_audits.md`); standalone by design. |
| `source-backed-numbers` | [fbdeme/source-backed-numbers](https://github.com/fbdeme/source-backed-numbers) | Never hand-type an experiment number into a document. Experiments emit **per-item JSON**; a generator turns it into `\input`-able LaTeX tables + `\newcommand{\numX}{..}` macros; prose uses `\numX`, never a literal; regenerate on data change and grep-audit for stray digits. Non-JSON numbers (from a checkpoint) get a reproducer that recomputes them into JSON. Distilled from a write-up where hand-transcription had silently introduced five wrong numbers. Companion to `academic-paper-reviewer` / `paper-style-guide`. |
| `stat-tone` | [fbdeme/stat-tone](https://github.com/fbdeme/stat-tone) | "Is the statistical claim stronger than the evidence?" — over-claiming checklist for ML/benchmark-eval writing: test-set selection bias (incl. by-construction identities), test-dependent significance counts (the same data gives different k under Welch vs Wilcoxon; n=5 Wilcoxon floor p=.0625), effect-size vs survivor-count anchoring, seed-replicate degeneracy, matrix-wide multiple comparisons. Empirical-ML complement to classical stats-reporting review; companion to `academic-paper-reviewer`'s methodology + artifact-auditor agents. |
| `typed-todo` | [fbdeme/typed-todo](https://github.com/fbdeme/typed-todo) | Cross-project personal task management as a typed property graph — 5 classes (Task/Project/Area/Person/Resource), 7 typed object properties with explicit cardinality, markdown vault at `~/todo/`. Companion to `docs-pattern` (per-project) and `obsidian-wiki` (knowledge): this one manages *intentions*. |
| `vast-gpu` | [fbdeme/vast-gpu](https://github.com/fbdeme/vast-gpu) | Rent, drive, and safely tear down [vast.ai](https://vast.ai) GPU instances from a GPU-less machine. One-file `remote.sh` wrapping the `vastai` CLI via `uv` (never installed); code is rsync'd not cloned (no git/vast/HF creds on the box), `uv sync` + a mandatory `cuda: True` check catches silent CPU fallback, detached `setsid` run pattern, and a **verify-before-trust `down`** (destroy `--yes` then re-query the API — a false "destroyed" once ran two idle boxes for hours). Gotchas doc = one wasted rental per rule. |
| `webprotege-selfhost` | [fbdeme/webprotege-selfhost](https://github.com/fbdeme/webprotege-selfhost) | Self-host [WebProtégé](https://github.com/protegeproject/webprotege) (Docker) to browse/visualize an OWL/RDF ontology. Pins `mongo:4.0` (the 2019 image's legacy driver breaks on mongo 5+) and seeds MongoDB for the two things that image needs before it's usable: clear "WebProtégé is not configured properly" (`ApplicationPreferences`) and enable the hidden Sign-up + project create/upload (`RoleAssignments` — account/project creation is RBAC-gated, not config). Ships a TBox+ABox→`.owl` (rdflib) merge helper and a decompiled `internals.md` so the seeds are trustworthy/adaptable. |
| `webprotege-cli` | [fbdeme/webprotege-cli-skill](https://github.com/fbdeme/webprotege-cli-skill) | Drive a self-hosted [WebProtégé](https://github.com/protegeproject/webprotege) from the CLI/agents (the 2019 image has no REST API) and edit OWL ontologies safely. `wp` (Node + Playwright headless browser) does signup/login/list/create-from-file/export/`apply-edits`; `onto` (Python rdflib + owlready2) is a structured, validated edit engine — add classes/properties/individuals/annotations + disjointness/characteristics/inverses, remove, `validate --reason` (auto-relaxes datatypes outside the OWL 2 map), SPARQL, and `diff` (a structural round-trip differential that exits non-zero on silent loss) — that **refuses to reference undeclared entities** (anti-hallucination) and preserves the ontology IRI. Hybrid loop: edit a canonical file, push to WebProtégé with `apply-edits`; the boundary is lossy by design (it silently mangles `@`-literals and drops RDF reification), so verify any round-trip with `onto diff`. Wraps [fbdeme/webprotege-cli](https://github.com/fbdeme/webprotege-cli) (the tool); companion to `webprotege-selfhost`. |
| `model-registry` | [fbdeme/model-registry](https://github.com/fbdeme/model-registry) (private) | One registry (`models.json`, the "grocery") of every model this machine can use — alias, provider, connection params, context window **measured from the upstream** (vLLM `max_model_len` / OpenRouter `context_length`, with source+date), cost, orchestrator/worker role, verification status — from which the LiteLLM config (generated), the request router's local set, the `/model` picker rows, the backend switch's window, and the worker launcher's env are derived. `render --check` catches drift. Tooling in `claude-code-model-backend`. |
| `backend-switch` | [fbdeme/backend-switch](https://github.com/fbdeme/backend-switch) (private) | Switch the **orchestration session's** default backend between the Anthropic subscription (OAuth) and a local/proxied model by rendering `settings.json` from `backends.json` + the grocery, with gates (orchestrator role, window ≥ 1M, subscription models only via `sub` + `/model`), and switch back without losing the `/model` choice. Encodes the traps that cost real sessions (settings env beating both `settings.model` and worker launchers; the global window override; base URL read once at boot; Remote Control dying under a custom base URL). |
| `worker-delegation` | [fbdeme/worker-delegation](https://github.com/fbdeme/worker-delegation) (private) | Judge/worker split for delegating work to a cheap or local model: the judge keeps the answer key, the worker (separate `claude -p` on a local alias) does census-type work a deterministic script can grade. Gate (no verifier → no delegation; census vs verdict), 7-block brief, canonical run line (`modelUsage` proves who answered), grading scripts, and a trap list where every entry cost a real session. |
| `local-llm-backend` | [fbdeme/local-llm-backend](https://github.com/fbdeme/local-llm-backend) (private) | Keep the Claude Code harness, swap only the engine to a local vLLM (DGX Spark, a company Funnel endpoint): LiteLLM proxy + `ANTHROPIC_BASE_URL` + `hosted_vllm/` + system-message shim + `enable_thinking:false` + the `tool_choice`/empty-tools 400 patch. Plumbing only — model list is `model-registry`, switching is `backend-switch`, delegating is `worker-delegation`. |
| `citation-check` | [fbdeme/citation-check](https://github.com/fbdeme/citation-check) (private) | A deterministic audit of a LaTeX paper's citation *wiring*: undefined cites, unused bib entries, duplicate keys, citekey/year drift |
| `paperbanana-figure` | [fbdeme/paperbanana-figure](https://github.com/fbdeme/paperbanana-figure) (private) | Generate a paper's CONCEPTUAL / schematic figure (method overview, framework, pipeline teaser) as a polished raster image via PaperBanana (multi-agent LLM + Gemini/nano-banana image gen), then pick the best of N candidates, image-to-image refine it to 2K, verify every label survived, and integrate i |
| `source-backed-figure` | [fbdeme/source-backed-figure](https://github.com/fbdeme/source-backed-figure) (private) | Draw a paper's DATA figures (line/bar/scatter plots of experimental results) as matplotlib vector PDF, with the numbers READ from committed eval JSON/CSV -- never hand-typed into the plotting code |
| `patent-register` | [fbdeme/patent-register](https://github.com/fbdeme/patent-register) (private) | 국문 특허 명세서(KIPO)의 문체(register)를 진단·교정한다 |
| `skill-search` | [fbdeme/skill-search](https://github.com/fbdeme/skill-search) (private) | MANDATORY prior-art search before creating any new skill (or any substantial reusable tool): sweep the user's OWN assets first (~/.claude/skills, fbdeme repos, memory, notes), then official sources, registries with tested APIs, GitHub, and community — verify every candidate by fetching its actual co |
| `notion-concept-wiki` | [fbdeme/notion-concept-wiki](https://github.com/fbdeme/notion-concept-wiki) (private) | 개인 개념 사전을 Notion에 유지하는 스킬 (복습/암기 시스템 아님) |
| `notion-project-memory` | [fbdeme/notion-project-memory](https://github.com/fbdeme/notion-project-memory) (private) | docs-pattern 프로젝트 문서(docs/)를 Notion 프로젝트 페이지에 **미러**로 발행하는 스킬 |
| `train-run` | [fbdeme/train-run](https://github.com/fbdeme/train-run) (private) | Launch, monitor, and judge ML training runs without burning compute on a silently-wrong run |
| `design-brad-frost` | [fbdeme/design-brad-frost](https://github.com/fbdeme/design-brad-frost) (private) | Atomic Design interface inventory & audit |
| `paper-recompose` | [fbdeme/paper-recompose](https://github.com/fbdeme/paper-recompose) (private) | Decompose an accretion-bloated paper and rebuild it from a recomposition map — for the failure mode where successive review responses each ADDED qualifiers, mirrors, and pointer webs until the paper reads as "a pile of AI-run experiments" |
| `skeleton-prose` | [fbdeme/skeleton-prose](https://github.com/fbdeme/skeleton-prose) (private) | Write paper prose skeleton-first, with a DETERMINISTIC claim↔prose binder |

## Native commands (slash commands)

Commands install into `.claude/commands/` instead of `.claude/skills/` and are invoked by typing `/<name>`.

| Command | Repo | What it does |
|---|---|---|
| `/pptx` | [fbdeme/claude-pptx-skill](https://github.com/fbdeme/claude-pptx-skill) | Generate professional PowerPoint via python-pptx. **§0 content-structure gate runs before any code** (governing thought → dot-dash outline with user approval → ghost-deck tests → relation→diagram mapping, bullets as last resort → script-measured text budgets), then design rules (NAVY/BLUE single-accent palette, native paragraph-level bullets, table styling, LibreOffice PNG preview). §0's deeper rules load on demand from the [`pptx-subskills/`](./pptx-subskills) library below. |

## Mirrored bundles (third-party)

Pinned snapshots of upstream skill bundles I use. **Original LICENSE files are preserved inside each subdirectory** (MIT or Apache-2.0). These copies are **not auto-synced** — pull from upstream directly if you want the latest.

| Bundle | Upstream | Contains | What it does |
|---|---|---|---|
| [`gstack/`](./gstack) | [garrytan/gstack](https://github.com/garrytan/gstack) | 50+ skills (`autoplan`, `benchmark`, `design-review`, `qa`, etc.) + supporting `bin/` scripts | Garry Tan's opinionated Claude Code stack — 23+ tools playing CEO / Designer / Eng Manager / Release Manager / Doc Engineer / QA roles. Many skills depend on the bundle's own `bin/` scripts, so install the whole `gstack/` rather than cherry-picking individual skill folders. |
| [`obsidian-wiki/`](./obsidian-wiki) | [Ar9av/obsidian-wiki](https://github.com/Ar9av/obsidian-wiki) | ~37 skills under `.skills/` (`llm-wiki`, `skill-creator`, `wiki-*`, `*-history-ingest`) + a Python package `obsidian_wiki/` | Framework for AI agents to maintain a "digital brain" via an Obsidian vault, following Karpathy's LLM Wiki pattern. The skills under `.skills/` are usable standalone; many also expect the Python package + `setup.sh` for full ingestion/sync functionality. |
| [`ponytail/`](./ponytail) | [DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail) | 6 skills under `skills/` (`ponytail`, `ponytail-audit`, `ponytail-review`, `ponytail-debt`, `ponytail-gain`, `ponytail-help`) + LICENSE + README | "Laziest senior dev" anti-over-engineering. A decision ladder that writes the least code that solves the problem, plus repo/diff audits ranking what to `delete`/`shrink`/replace-with-`stdlib`. Skill folders only — the full upstream also ships plugin hooks/commands/MCP for auto-activation. Self-contained markdown skills; usable standalone. |
| [`frontend-design/`](./frontend-design) | [anthropics/skills](https://github.com/anthropics/skills) @ `fa0fa64` (Apache-2.0) | 1 skill, `SKILL.md` at dir root | Anthropic's official anti-"AI slop" design skill: act as an opinionated design lead — ground aesthetics in the subject's world, spend boldness in one place, avoid the three clustered defaults (cream+serif, near-black+neon, broadsheet). |
| [`interface-design/`](./interface-design) | [Dammyjay93/interface-design](https://github.com/Dammyjay93/interface-design) @ `2f9be32` (MIT) | 1 skill (`SKILL.md` + `agents/`) | Craft-first **product UI** design (dashboards/tools/data interfaces — not marketing). Persists decisions to `.interface-design/system.md` so spacing/tokens/component styles survive across sessions; `/interface-design:design-review` for hierarchy audits. |
| [`impeccable/`](./impeccable) | [pbakaus/impeccable](https://github.com/pbakaus/impeccable) @ `4d849eb` v3.9.1 (Apache-2.0) | 1 skill (`SKILL.md` + `reference/` + `scripts/`) | Paul Bakaus' design guidance system: ~20 commands (`craft`, `audit`, `critique`, `polish`, `bolder`, `quieter`, `animate`, `harden`, …) + deterministic anti-pattern detectors (46 rules). Snapshot is the prebuilt plugin skill; upstream also ships hooks/agents via `npx impeccable install`. |
| [`ui-ux-pro-max/`](./ui-ux-pro-max) | [nextlevelbuilder/ui-ux-pro-max-skill](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill) @ `1307d97` v2.11.0 (MIT) | 1 skill (`SKILL.md` + `data/` CSVs + `scripts/search.py`) | Design-system generator: searchable local DB of 84 UI styles, 192 palettes, 74 font pairings, 98 UX guidelines, 25 chart types across 22 stacks. `scripts/search.py "<query>" --design-system` reasons out a full tailored system (pattern/style/colors/type/anti-patterns). Python stdlib only. |
| [`korean-humanizer/`](./korean-humanizer) | [NomaDamas/k-skill](https://github.com/NomaDamas/k-skill) @ `42473da` (MIT) | 1 skill (`SKILL.md` + `references/`) | De-AI-ify Korean prose: A–J pattern taxonomy (60+ sub-patterns) with S1/S2/S3 severity, translation-ese + stock-phrase detection, meaning-preservation guards (30% change warning / 50% abort). Snapshot includes a clearly-marked local extension, `references/deck-supplement.md` — genre overrides for slides/개조식 reports (symbol substitution table, 개조식 conversion rules, header dualization) distilled from a 2026-07 survey of Korean business-deck conventions. |
| [`pptx-subskills/`](./pptx-subskills) | 5 upstreams — see [its README](./pptx-subskills/README.md) | 3 mirrored bundles (MIT): [Gabberflast/academic-pptx-skill](https://github.com/Gabberflast/academic-pptx-skill) @ `9f2b703`, [addsumtech/slides_maker](https://github.com/addsumtech/slides_maker) @ `fa666c3`, [seulee26/mckinsey-pptx](https://github.com/seulee26/mckinsey-pptx) @ `201ef49`; 2 link-only (no license): [nraford7/Narrative-Engine](https://github.com/nraford7/Narrative-Engine) @ `3a7aa75`, [appautomaton/presentation](https://github.com/appautomaton/presentation) @ `3a39938` | Content-quality sub-skill library for `/pptx`'s §0 gate — the gate coordinates, and loads per stage: deck brief + word-count script (slides_maker), headline rules (Narrative-Engine), assertion-evidence + outline approval (academic-pptx-skill), vertical-logic/ghost-deck tests (presentation), 40-template catalog + real deck builder (mckinsey-pptx). Sub-agent-style progressive loading: read only the file the current stage needs. |

## Install (per project)

For **native skills/commands**, use the install command on the skill's own README. They publish their own tarballs at the repo root, so the strip-components dance is simpler than going through this archive.

For a **single-skill mirror** (`frontend-design/`, `interface-design/`, `impeccable/`, `ui-ux-pro-max/` — `SKILL.md` at dir root), the dir itself is the skill, so strip=1:

```bash
mkdir -p ~/.claude/skills
curl -sL https://github.com/fbdeme/claude-skills/archive/main.tar.gz \
  | tar xz -C ~/.claude/skills --strip-components=1 \
      claude-skills-main/<skill-name>
```

For a **single skill from a mirrored bundle**, pull it out of this archive's tarball:

```bash
# From gstack/ — path is claude-skills-main/gstack/<skill>, so strip=2
mkdir -p .claude/skills
curl -sL https://github.com/fbdeme/claude-skills/archive/main.tar.gz \
  | tar xz -C .claude/skills --strip-components=2 \
      claude-skills-main/gstack/<skill-name>

# From obsidian-wiki/.skills/ — path is claude-skills-main/obsidian-wiki/.skills/<skill>, so strip=3
mkdir -p .claude/skills
curl -sL https://github.com/fbdeme/claude-skills/archive/main.tar.gz \
  | tar xz -C .claude/skills --strip-components=3 \
      claude-skills-main/obsidian-wiki/.skills/<skill-name>
```

Heads up: gstack skills often call into `gstack/bin/*` scripts; pulling a single gstack skill without `bin/` may leave it half-broken. When in doubt, pull the whole `gstack/` directory.

## Adding a new skill

For a new native skill: spin up a new `fbdeme/<skill>` repo with `SKILL.md` at its root, then add a row to the table above linking to it.

For a new mirrored bundle: drop a snapshot under `<name>/`, preserve the upstream `LICENSE`, and add a row to the bundles table with the upstream link and a one-liner.

## License

MIT for the index itself ([`LICENSE`](./LICENSE)). Mirrored bundles retain their own upstream licenses.
